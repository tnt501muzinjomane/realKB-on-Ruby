<%= form_for(@solutionsdisplay) do |f| %>
  <% if @solutionsdisplay.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solutionsdisplay.errors.count, "error") %> prohibited this solutionsdisplay from being saved:</h2>

      <ul>
      <% @solutionsdisplay.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
